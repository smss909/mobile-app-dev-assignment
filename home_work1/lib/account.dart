import 'package:flutter/material.dart';

class AccountE extends StatelessWidget {
  const AccountE({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
            child: Text("Account Page", style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold),))
    );
  }
}
